{
  "title": "Google Maps Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchTerm": {
      "title": "Search term",
      "type": "string",
      "description": "e.g. 'photographer', 'fitnessstudio', 'tätowierer'",
      "editor": "textfield"
    },
    "latitude": {
      "title": "Latitude",
      "type": "string",
      "description": "Latitude coordinate of the center point"
    },
    "longitude": {
      "title": "Longitude",
      "type": "string",
      "description": "Longitude coordinate of the center point"
    },
    "radiusKm": {
      "title": "Radius in Kilometern",
      "type": "integer",
      "description": "Radius around the coordinates in kilometers",
      "default": 50
    },
    "limit": {
      "title": "Max. Anzahl an Ergebnissen",
      "type": "integer",
      "description": "Maximale Anzahl von Einträgen, die extrahiert werden sollen",
      "default": 10
    }
  },
  "required": ["searchTerm"]
}
