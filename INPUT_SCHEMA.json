{
  "title": "Google Maps Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchStringsArray": {
      "title": "Suchbegriffe",
      "type": "array",
      "description": "Begriffe, nach denen auf Google Maps gesucht werden soll.",
      "editor": "stringList"
    },
    "locationQuery": {
      "title": "Ort (optional)",
      "type": "string",
      "editor": "textfield",
      "description": "z. B. 'Ulm, Deutschland' – wird verwendet, wenn keine Koordinaten angegeben sind."
    },
    "latitude": {
      "title": "Breitengrad",
      "type": "string",
      "editor": "textfield",
      "description": "z. B. 48.308727 – wird verwendet, wenn kein Ort angegeben ist."
    },
    "longitude": {
      "title": "Längengrad",
      "type": "string",
      "editor": "textfield",
      "description": "z. B. 9.903306 – wird verwendet, wenn kein Ort angegeben ist."
    },
    "radius": {
      "title": "Suchradius (in km)",
      "type": "integer",
      "editor": "number",
      "description": "z. B. 50"
    },
    "maxCrawledPlaces": {
      "title": "Maximale Anzahl Ergebnisse",
      "type": "integer",
      "editor": "number",
      "description": "z. B. 10"
    }
  },
  "required": [
    "searchStringsArray",
    "radius",
    "maxCrawledPlaces"
  ],
  "anyOf": [
    {
      "required": ["locationQuery"]
    },
    {
      "required": ["latitude", "longitude"]
    }
  ]
}
